/* 
4. Escribe un programa que declare variables de tipo char y de tipo String. 
   Intenta mostrarlas por pantalla todas juntas en la misma línea y con una sola sentencia.
*/

let variablString1 = String("hola");
let variableNumber = Number(69.5);
let variableBigInt = BigInt(500)

console.log(variablString1 + " - " + variableNumber + " - " + variableBigInt);